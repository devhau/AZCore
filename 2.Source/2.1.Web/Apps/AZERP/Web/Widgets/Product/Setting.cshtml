@model ProductWidget

    <div class="row">
        <div class="col-md-6 form-group">
            <az-text-model model="Model.Setting" func="(item)=>((ProductWidgetSetting)item).Title" value-default="Model.Title" is-check-default-input-null="true" label="Tiêu đề" />
        </div>
        <div class="col-md-6  form-group">
            <az-widget-width model="Model.Setting" func="(item)=>((ProductWidgetSetting)item).WidthClass" label="Độ rộng" />
        </div>
        <div class="col-md-6  form-group">
            <az-text-model model="Model.Setting" func="(item)=>((ProductWidgetSetting)item).Icon" label="Biểu tượng" />
        </div>
        <div class="col-md-6  form-group">
            <az-text-model model="Model.Setting" func="(item)=>((ProductWidgetSetting)item).Icon" label="Biểu tượng" />
        </div>
        <div class="col-md-6  form-group">
            <az-widget-type-select model="Model.Setting" func="(item)=>((ProductWidgetSetting)item).Type" label="Kiểu hiển thị" is-null-first="false" />
        </div>
    </div>